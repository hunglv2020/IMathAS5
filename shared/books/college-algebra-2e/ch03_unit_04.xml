<?xml version="1.0" encoding="UTF-8"?>
<section>
  <heading level="1">3.4 Composition of Functions</heading>
  <heading level="2">Learning Objectives</heading>
  <list type="unordered">
    <item>Combine functions using algebraic operations.</item>
    <item>Create a new function by composition of functions.</item>
    <item>Evaluate composite functions.</item>
    <item>Find the domain of a composite function.</item>
    <item>Decompose a composite function into its component functions.</item>
  </list>
  <paragraph>Suppose we want to calculate how much it costs to heat a house on a particular day of the year. The cost to heat a house will depend on the average daily temperature, and in turn, the average daily temperature depends on the particular day of the year. Notice how we have just defined two relationships: The cost depends on the temperature, and the temperature depends on the day.</paragraph>
  <paragraph>Using descriptive variables, we can notate these two functions. The function $$C(T)$$ gives the cost $$C$$ of heating a house for a given average daily temperature in $$T$$ degrees Celsius. The function $$T(d)$$ gives the average daily temperature on day $$d$$ of the year. For any given day, $$\text{Cost} = C\left( {T(d)} \right)$$ means that the cost depends on the temperature, which in turns depends on the day of the year. Thus, we can evaluate the cost function at the temperature $$T(d).$$ For example, we could evaluate $$T(5)$$ to determine the average daily temperature on the 5th day of the year. Then, we could evaluate the cost function at that temperature. We would write $$C\left( {T(5)} \right).$$</paragraph>
  <paragraph>By combining these two relationships into one function, we have performed function composition, which is the focus of this section.</paragraph>
  <heading level="2">Combining Functions Using Algebraic Operations</heading>
  <paragraph>Function composition is only one way to combine existing functions. Another way is to carry out the usual algebraic operations on functions, such as addition, subtraction, multiplication and division. We do this by performing the operations with the function outputs, defining the result as the output of our new function.</paragraph>
  <paragraph>Suppose we need to add two columns of numbers that represent a husband and wife’s separate annual incomes over a period of years, with the result being their total household income. We want to do this for every year, adding only that year’s incomes and then collecting all the data in a new column. If $$w(y)$$ is the wife’s income and $$h(y)$$ is the husband’s income in year $$y,$$ and we want $$T$$ to represent the total income, then we can define a new function.</paragraph>
  <paragraph math="block">$$T(y) = h(y) + w(y)$$</paragraph>
  <paragraph>If this holds true for every year, then we can focus on the relation between the functions without reference to a year and write</paragraph>
  <paragraph math="block">$$T = h + w$$</paragraph>
  <paragraph>Just as for this sum of two functions, we can define difference, product, and ratio functions for any pair of functions that have the same kinds of inputs (not necessarily numbers) and also the same kinds of outputs (which do have to be numbers so that the usual operations of algebra can apply to them, and which also must have the same units or no units when we add and subtract). In this way, we can think of adding, subtracting, multiplying, and dividing functions.</paragraph>
  <paragraph>For two functions $$f(x)$$ and $$g(x)$$ with real number outputs, we define new functions $$f + g, f - g, fg,$$ and $$\frac{f}{g}$$ by the relations</paragraph>
  <paragraph math="block">$$\begin{array}{rclc} {(f + g)(x)} &amp; = &amp; {f(x) + g(x)} &amp; \\ {(f - g)(x)} &amp; = &amp; {f(x) - g(x)} &amp; \\ { (fg)(x)} &amp; = &amp; {f(x)g(x)} &amp; \\ {\left( \frac{f}{g} \right)(x)} &amp; = &amp; \frac{f(x)}{g(x)} &amp; { \text{where} g(x) \neq 0} \end{array}$$</paragraph>
  <example number="1" title="Performing Algebraic Operations on Functions">
    <problem>
      <paragraph>Find and simplify the functions $$\left( {g - f} \right)(x)$$ and $$\left( \frac{g}{f} \right)(x),$$ given $$f(x) = x - 1$$ and $$g(x) = x^{2} - 1.$$ Are they the same function?</paragraph>
    </problem>
    <solution>
      <paragraph>Begin by writing the general form, and then substitute the given functions.</paragraph>
      <paragraph math="block">$$\begin{matrix} {(g - f)(x)} &amp; = &amp; {g(x) - f(x)} &amp; \\ {(g - f)(x)} &amp; = &amp; {x^{2} - 1 - (x - 1)} &amp; \\ {(g - f)(x)} &amp; = &amp; {x^{2} - x} &amp; \\ {(g - f)(x)} &amp; = &amp; {x(x - 1)} &amp; \\ &amp; &amp; &amp; \\ &amp; &amp; &amp; \\ {\left( \frac{g}{f} \right)(x)} &amp; = &amp; \frac{g(x)}{f(x)} &amp; \\ {\left( \frac{g}{f} \right)(x)} &amp; = &amp; \frac{x^{2} - 1}{x - 1} &amp; \\ {\left( \frac{g}{f} \right)(x)} &amp; = &amp; \frac{(x + 1)(x - 1)}{x - 1} &amp; { \text{where} x \neq 1} \\ {\left( \frac{g}{f} \right)(x)} &amp; = &amp; {x + 1} &amp; \end{matrix}$$</paragraph>
      <paragraph>No, the functions are not the same.</paragraph>
      <paragraph>Note: For $$\left( \frac{g}{f} \right)(x),$$ the condition $$x \neq 1$$ is necessary because when $$x = 1,$$ the denominator is equal to 0, which makes the function undefined.</paragraph>
    </solution>
  </example>
  <note type="practice" source_type="Try It" title="Try It" number="#1">
    <paragraph>Find and simplify the functions $$\left( {fg} \right)(x)$$ and $$\left( {f - g} \right)(x).$$</paragraph>
    <paragraph math="block">$$f(x) = x - 1 \text{and} g(x) = x^{2} - 1$$</paragraph>
    <paragraph>Are they the same function?</paragraph>
  </note>
  <heading level="2">Create a Function by Composition of Functions</heading>
  <paragraph>Performing algebraic operations on functions combines them into a new function, but we can also create functions by composing functions. When we wanted to compute a heating cost from a day of the year, we created a new function that takes a day as input and yields a cost as output. The process of combining functions so that the output of one function becomes the input of another is known as a composition of functions. The resulting function is known as a composite function. We represent this combination by the following notation:</paragraph>
  <paragraph math="block">$$\left( {f \circ g} \right)(x) = f\left( {g(x)} \right)$$</paragraph>
  <paragraph>We read the left-hand side as $$``f$$ composed with $$g$$ at $$x \text{,"}$$ and the right-hand side as $$``f$$ of $$g$$ of $$x."$$ The two sides of the equation have the same mathematical meaning and are equal. The open circle symbol $$\circ$$ is called the composition operator. We use this operator mainly when we wish to emphasize the relationship between the functions themselves without referring to any particular input value. Composition is a binary operation that takes two functions and forms a new function, much as addition or multiplication takes two numbers and gives a new number. However, it is important not to confuse function composition with multiplication because, as we learned above, in most cases $$f(g(x)) \neq f(x)g(x).$$</paragraph>
  <paragraph>It is also important to understand the order of operations in evaluating a composite function. We follow the usual convention with parentheses by starting with the innermost parentheses first, and then working to the outside. In the equation above, the function $$g$$ takes the input $$x$$ first and yields an output $$g(x).$$ Then the function $$f$$ takes $$g(x)$$ as an input and yields an output $$f\left( {g(x)} \right).$$</paragraph>
  <paragraph>In general, $$f \circ g$$ and $$g \circ f$$ are different functions. In other words, in many cases $$f\left( {g(x)} \right) \neq g\left( {f(x)} \right)$$ for all $$x.$$ We will also see that sometimes two functions can be composed only in one specific order.</paragraph>
  <paragraph>For example, if $$f(x) = x^{2}$$ and $$g(x) = x + 2,$$ then</paragraph>
  <paragraph math="block">$$\begin{array}{ccl} {f(g(x))} &amp; = &amp; {f(x + 2)} \\ &amp; = &amp; {(x + 2)}^{2} \\ &amp; = &amp; {x^{2} + 4x + 4} \end{array}$$</paragraph>
  <paragraph>but</paragraph>
  <paragraph math="block">$$\begin{array}{ccl} {g(f(x))} &amp; = &amp; {g\left( x^{2} \right)} \\ &amp; = &amp; {x^{2} + 2} \end{array}$$</paragraph>
  <paragraph>These expressions are not equal for all values of $$x,$$ so the two functions are not equal. It is irrelevant that the expressions happen to be equal for the single input value $$x = - \frac{1}{2}.$$</paragraph>
  <paragraph>Note that the range of the inside function (the first function to be evaluated) needs to be within the domain of the outside function. Less formally, the composition has to make sense in terms of inputs and outputs.</paragraph>
  <note type="rule" source_type="Composition of Functions" title="Composition of Functions">
    <paragraph>When the output of one function is used as the input of another, we call the entire operation a composition of functions. For any input $$x$$ and functions $$f$$ and $$g,$$ this action defines a composite function, which we write as $$f \circ g$$ such that</paragraph>
    <paragraph math="block">$$\left( {f \circ g} \right)(x) = f\left( {g(x)} \right)$$</paragraph>
    <paragraph>The domain of the composite function $$f \circ g$$ is all $$x$$ such that $$x$$ is in the domain of $$g$$ and $$g(x)$$ is in the domain of $$f.$$</paragraph>
    <paragraph>It is important to realize that the product of functions $$fg$$ is not the same as the function composition $$f\left( {g(x)} \right),$$ because, in general, $$f(x)g(x) \neq f\left( {g(x)} \right).$$</paragraph>
  </note>
  <example number="2" title="Determining whether Composition of Functions is Commutative">
    <problem>
      <paragraph>Using the functions provided, find $$f\left( {g(x)} \right)$$ and $$g\left( {f(x)} \right).$$ Determine whether the composition of the functions is commutative.</paragraph>
      <paragraph math="block">$$f(x) = 2x + 1 g(x) = 3 - x$$</paragraph>
    </problem>
    <solution>
      <paragraph>Let’s begin by substituting $$g(x)$$ into $$f(x).$$</paragraph>
      <paragraph math="block">$$\begin{array}{ccl} {f(g(x))} &amp; = &amp; {2(3 - x) + 1} \\ &amp; = &amp; {6 - 2x + 1} \\ &amp; = &amp; {7 - 2x} \end{array}$$</paragraph>
      <paragraph>Now we can substitute $$f(x)$$ into $$g(x).$$</paragraph>
      <paragraph math="block">$$\begin{array}{ccl} {g(f(x))} &amp; = &amp; {3 - (2x + 1)} \\ &amp; = &amp; {3 - 2x - 1} \\ &amp; = &amp; {-2x + 2} \end{array}$$</paragraph>
      <paragraph>We find that $$g(f(x)) \neq f(g(x)),$$ so the operation of function composition is not commutative.</paragraph>
    </solution>
  </example>
  <example number="3" title="Interpreting Composite Functions">
    <problem>
      <paragraph>The function $$c(s)$$ gives the number of calories burned completing $$s$$ sit-ups, and $$s(t)$$ gives the number of sit-ups a person can complete in $$t$$ minutes. Interpret $$c(s(3)).$$</paragraph>
    </problem>
    <solution>
      <paragraph>The inside expression in the composition is $$s(3).$$ Because the input to the s-function is time, $$t = 3$$ represents 3 minutes, and $$s(3)$$ is the number of sit-ups completed in 3 minutes.</paragraph>
      <paragraph>Using $$s(3)$$ as the input to the function $$c(s)$$ gives us the number of calories burned during the number of sit-ups that can be completed in 3 minutes, or simply the number of calories burned in 3 minutes (by doing sit-ups).</paragraph>
    </solution>
  </example>
  <example number="4" title="Investigating the Order of Function Composition">
    <problem>
      <paragraph>Suppose $$f(x)$$ gives miles that can be driven in $$x$$ hours and $$g(y)$$ gives the gallons of gas used in driving $$y$$ miles. Which of these expressions is meaningful: $$f\left( {g(y)} \right)$$ or $$g\left( {f(x)} \right)?$$</paragraph>
    </problem>
    <solution>
      <paragraph>The function $$y = f(x)$$ is a function whose output is the number of miles driven corresponding to the number of hours driven.</paragraph>
      <paragraph math="block">$$\text{number of miles} = f( \text{number of hours} )$$</paragraph>
      <paragraph>The function $$g(y)$$ is a function whose output is the number of gallons used corresponding to the number of miles driven. This means:</paragraph>
      <paragraph math="block">$$\text{number of gallons} = g( \text{number of miles} )$$</paragraph>
      <paragraph>The expression $$g(y)$$ takes miles as the input and a number of gallons as the output. The function $$f(x)$$ requires a number of hours as the input. Trying to input a number of gallons does not make sense. The expression $$f\left( {g(y)} \right)$$ is meaningless.</paragraph>
      <paragraph>The expression $$f(x)$$ takes hours as input and a number of miles driven as the output. The function $$g(y)$$ requires a number of miles as the input. Using $$f(x)$$ (miles driven) as an input value for $$g(y),$$ where gallons of gas depends on miles driven, does make sense. The expression $$g\left( {f(x)} \right)$$ makes sense, and will yield the number of gallons of gas used, $$g,$$ driving a certain number of miles, $$f(x),$$ in $$x$$ hours.</paragraph>
    </solution>
  </example>
  <note type="sidebar" source_type="Q&amp;A" title="Q&amp;A">
    <paragraph>Are there any situations where $$f(g(y))$$ and $$g(f(x))$$ would both be meaningful or useful expressions?</paragraph>
    <paragraph>Yes. For many pure mathematical functions, both compositions make sense, even though they usually produce different new functions. In real-world problems, functions whose inputs and outputs have the same units also may give compositions that are meaningful in either order.</paragraph>
  </note>
  <note type="practice" source_type="Try It" title="Try It" number="#2">
    <paragraph>The gravitational force on a planet a distance r from the sun is given by the function $$G(r).$$ The acceleration of a planet subjected to any force $$F$$ is given by the function $$a(F).$$ Form a meaningful composition of these two functions, and explain what it means.</paragraph>
  </note>
  <heading level="2">Evaluating Composite Functions</heading>
  <paragraph>Once we compose a new function from two existing functions, we need to be able to evaluate it for any input in its domain. We will do this with specific numerical inputs for functions expressed as tables, graphs, and formulas and with variables as inputs to functions expressed as formulas. In each case, we evaluate the inner function using the starting input and then use the inner function’s output as the input for the outer function.</paragraph>
  <heading level="3">Evaluating Composite Functions Using Tables</heading>
  <paragraph>When working with functions given as tables, we read input and output values from the table entries and always work from the inside to the outside. We evaluate the inside function first and then use the output of the inside function as the input to the outside function.</paragraph>
  <example number="5" title="Using a Table to Evaluate a Composite Function">
    <problem>
      <paragraph>Using Table 1, evaluate $$f(g(3))$$ and $$g(f(3)).$$</paragraph>
      <table>
        <thead>
          <row>
            <cell header="true">$$x$$</cell>
            <cell header="true">$$f(x)$$</cell>
            <cell header="true">$$g(x)$$</cell>
          </row>
        </thead>
        <tbody>
          <row>
            <cell>1</cell>
            <cell>6</cell>
            <cell>3</cell>
          </row>
          <row>
            <cell>2</cell>
            <cell>8</cell>
            <cell>5</cell>
          </row>
          <row>
            <cell>3</cell>
            <cell>3</cell>
            <cell>2</cell>
          </row>
          <row>
            <cell>4</cell>
            <cell>1</cell>
            <cell>7</cell>
          </row>
        </tbody>
      </table>
    </problem>
    <solution>
      <paragraph>To evaluate $$f(g(3) \text{),}$$ we start from the inside with the input value 3. We then evaluate the inside expression $$g(3)$$ using the table that defines the function $$g:$$ $$g(3) = 2.$$ We can then use that result as the input to the function $$f,$$ so $$g(3)$$ is replaced by 2 and we get $$f(2).$$ Then, using the table that defines the function $$f,$$ we find that $$f(2) = 8.$$</paragraph>
      <paragraph math="block">$$\begin{array}{rcl} {g(3)} &amp; = &amp; 2 \\ {f(g(3))} &amp; = &amp; {f(2) = 8} \end{array}$$</paragraph>
      <paragraph>To evaluate $$g(f(3) \text{),}$$ we first evaluate the inside expression $$f(3)$$ using the first table: $$f(3) = 3.$$ Then, using the table for $$g,$$ we can evaluate</paragraph>
      <paragraph math="block">$$g(f(3)) = g(3) = 2$$</paragraph>
      <paragraph>Table 2 shows the composite functions $$f \circ g$$ and $$g \circ f$$ as tables.</paragraph>
      <table>
        <tbody>
          <row>
            <cell>$$x$$</cell>
            <cell>$$g(x)$$</cell>
            <cell>$$f\left( {g(x)} \right)$$</cell>
            <cell>$$f(x)$$</cell>
            <cell>$$g\left( {f(x)} \right)$$</cell>
          </row>
          <row>
            <cell>3</cell>
            <cell>2</cell>
            <cell>8</cell>
            <cell>3</cell>
            <cell>2</cell>
          </row>
        </tbody>
      </table>
    </solution>
  </example>
  <note type="practice" source_type="Try It" title="Try It" number="#3">
    <paragraph>Using Table 1, evaluate $$f(g(1))$$ and $$g(f(4)).$$</paragraph>
  </note>
  <heading level="3">Evaluating Composite Functions Using Graphs</heading>
  <paragraph>When we are given individual functions as graphs, the procedure for evaluating composite functions is similar to the process we use for evaluating tables. We read the input and output values, but this time, from the $$x-$$ and $$y-$$ axes of the graphs.</paragraph>
  <note type="procedure" source_type="How To" title="How To">
    <paragraph>Given a composite function and graphs of its individual functions, evaluate it using the information provided by the graphs.</paragraph>
    <list type="ordered">
      <item>Locate the given input to the inner function on the $$x-$$ axis of its graph.</item>
      <item>Read off the output of the inner function from the $$y-$$ axis of its graph.</item>
      <item>Locate the inner function output on the $$x-$$ axis of the graph of the outer function.</item>
      <item>Read the output of the outer function from the $$y-$$ axis of its graph. This is the output of the composite function.</item>
    </list>
  </note>
  <example number="6" title="Using a Graph to Evaluate a Composite Function">
    <problem>
      <paragraph>Using Figure 1, evaluate $$f(g(1)).$$</paragraph>
      <figure number="1">
        <image alt="Explanation of the composite function."/>
      </figure>
    </problem>
    <solution>
      <paragraph>To evaluate $$f(g(1)),$$ we start with the inside evaluation. See Figure 2.</paragraph>
      <figure number="2">
        <image alt="Two graphs of a positive parabola (g(x)) and a negative parabola (f(x)). The following points are plotted: g(1)=3 and f(3)=6."/>
      </figure>
      <paragraph>We evaluate $$g(1)$$ using the graph of $$g(x),$$ finding the input of 1 on the $$x-$$ axis and finding the output value of the graph at that input. Here, $$g(1) = 3.$$ We use this value as the input to the function $$f.$$</paragraph>
      <paragraph math="block">$$f(g(1)) = f(3)$$</paragraph>
      <paragraph>We can then evaluate the composite function by looking to the graph of $$f(x),$$ finding the input of 3 on the $$x-$$ axis and reading the output value of the graph at this input. Here, $$f(3) = 6,$$ so $$f(g(1)) = 6.$$</paragraph>
    </solution>
    <paragraph>Figure 3 shows how we can mark the graphs with arrows to trace the path from the input value to the output value.</paragraph>
    <figure number="3">
      <image alt="Two graphs of a positive and negative parabola."/>
    </figure>
  </example>
  <note type="practice" source_type="Try It" title="Try It" number="#4">
    <paragraph>Using Figure 1, evaluate $$g(f(2)).$$</paragraph>
  </note>
  <heading level="3">Evaluating Composite Functions Using Formulas</heading>
  <paragraph>When evaluating a composite function where we have either created or been given formulas, the rule of working from the inside out remains the same. The input value to the outer function will be the output of the inner function, which may be a numerical value, a variable name, or a more complicated expression.</paragraph>
  <paragraph>While we can compose the functions for each individual input value, it is sometimes helpful to find a single formula that will calculate the result of a composition $$f\left( {g(x)} \right).$$ To do this, we will extend our idea of function evaluation. Recall that, when we evaluate a function like $$f(t) = t^{2} - t,$$ we substitute the value inside the parentheses into the formula wherever we see the input variable.</paragraph>
  <note type="procedure" source_type="How To" title="How To">
    <paragraph>Given a formula for a composite function, evaluate the function.</paragraph>
    <list type="ordered">
      <item>Evaluate the inside function using the input value or variable provided.</item>
      <item>Use the resulting output as the input to the outside function.</item>
    </list>
  </note>
  <example number="7" title="Evaluating a Composition of Functions Expressed as Formulas with a Numerical Input">
    <problem>
      <paragraph>Given $$f(t) = t^{2} - t$$ and $$h(x) = 3x + 2,$$ evaluate $$f(h(1)).$$</paragraph>
    </problem>
    <solution>
      <paragraph>Because the inside expression is $$h(1),$$ we start by evaluating $$h(x)$$ at 1.</paragraph>
      <paragraph math="block">$$\begin{array}{rcl} {h(1)} &amp; = &amp; {3(1) + 2} \\ {h(1)} &amp; = &amp; 5 \end{array}$$</paragraph>
      <paragraph>Then $$f(h(1)) = f(5),$$ so we evaluate $$f(t)$$ at an input of 5.</paragraph>
      <paragraph math="block">$$\begin{array}{rcl} {f(h(1))} &amp; = &amp; {f(5)} \\ {f(h(1))} &amp; = &amp; {5^{2} - 5} \\ {f(h(1))} &amp; = &amp; 20 \end{array}$$</paragraph>
    </solution>
    <paragraph>It makes no difference what the input variables $$t$$ and $$x$$ were called in this problem because we evaluated for specific numerical values.</paragraph>
  </example>
  <note type="practice" source_type="Try It" title="Try It" number="#5">
    <paragraph>Given $$f(t) = t^{2} - t$$ and $$h(x) = 3x + 2,$$ evaluate</paragraph>
    <list type="ordered">
      <item>ⓐ $$h(f(2))$$</item>
      <item>ⓑ $$h(f( - 2))$$</item>
    </list>
  </note>
  <heading level="2">Finding the Domain of a Composite Function</heading>
  <paragraph>As we discussed previously, the domain of a composite function such as $$f \circ g$$ is dependent on the domain of $$g$$ and the domain of $$f.$$ It is important to know when we can apply a composite function and when we cannot, that is, to know the domain of a function such as $$f \circ g.$$ Let us assume we know the domains of the functions $$f$$ and $$g$$ separately. If we write the composite function for an input $$x$$ as $$f\left( {g(x)} \right),$$ we can see right away that $$x$$ must be a member of the domain of $$g$$ in order for the expression to be meaningful, because otherwise we cannot complete the inner function evaluation. However, we also see that $$g(x)$$ must be a member of the domain of $$f,$$ otherwise the second function evaluation in $$f\left( {g(x)} \right)$$ cannot be completed, and the expression is still undefined. Thus the domain of $$f \circ g$$ consists of only those inputs in the domain of $$g$$ that produce outputs from $$g$$ belonging to the domain of $$f.$$ Note that the domain of $$f$$ composed with $$g$$ is the set of all $$x$$ such that $$x$$ is in the domain of $$g$$ and $$g(x)$$ is in the domain of $$f.$$</paragraph>
  <note type="rule" source_type="Domain of a Composite Function" title="Domain of a Composite Function">
    <paragraph>The domain of a composite function $$f\left( {g(x)} \right)$$ is the set of those inputs $$x$$ in the domain of $$g$$ for which $$g(x)$$ is in the domain of $$f.$$</paragraph>
  </note>
  <note type="procedure" source_type="How To" title="How To">
    <paragraph>Given a function composition $$f(g(x)),$$ determine its domain.</paragraph>
    <list type="ordered">
      <item>Find the domain of $$g.$$</item>
      <item>Find the domain of $$f.$$</item>
      <item>Find those inputs $$x$$ in the domain of $$g$$ for which $$g(x)$$ is in the domain of $$f.$$ That is, exclude those inputs $$x$$ from the domain of $$g$$ for which $$g(x)$$ is not in the domain of $$f.$$ The resulting set is the domain of $$f \circ g.$$</item>
    </list>
  </note>
  <example number="8" title="Finding the Domain of a Composite Function">
    <problem>
      <paragraph>Find the domain of</paragraph>
      <paragraph math="block">$$\left( {f \circ g} \right)(x) \text{where} f(x) = \frac{5}{x - 1} \text{and} g(x) = \frac{4}{3x - 2}$$</paragraph>
    </problem>
    <solution>
      <paragraph>The domain of $$g(x)$$ consists of all real numbers except $$x = \frac{2}{3},$$ since that input value would cause us to divide by 0. Likewise, the domain of $$f$$ consists of all real numbers except 1. So we need to exclude from the domain of $$g(x)$$ that value of $$x$$ for which $$g(x) = 1.$$</paragraph>
      <paragraph math="block">$$\begin{array}{rcl} \frac{4}{3x - 2} &amp; = &amp; 1 \\ 4 &amp; = &amp; {3x - 2} \\ 6 &amp; = &amp; {3x} \\ x &amp; = &amp; 2 \end{array}$$</paragraph>
      <paragraph>So the domain of $$f \circ g$$ is the set of all real numbers except $$\frac{2}{3}$$ and $$2.$$ This means that</paragraph>
      <paragraph math="block">$$x \neq \frac{2}{3} \text{or} x \neq 2$$</paragraph>
      <paragraph>We can write this in interval notation as</paragraph>
      <paragraph math="block">$$\left( {- \infty,\frac{2}{3}} \right) \cup \left( {\frac{2}{3},2} \right) \cup \left( {2,\infty} \right)$$</paragraph>
    </solution>
  </example>
  <example number="9" title="Finding the Domain of a Composite Function Involving Radicals">
    <problem>
      <paragraph>Find the domain of</paragraph>
      <paragraph math="block">$$\left( {f \circ g} \right)(x) \text{where} f(x) = \sqrt{x + 2} \text{and} g(x) = \sqrt{3 - x}$$</paragraph>
    </problem>
    <solution>
      <paragraph>Because we cannot take the square root of a negative number, the domain of $$g$$ is $$\left( {- \infty,3} \right\rbrack.$$ Now we check the domain of the composite function</paragraph>
      <paragraph math="block">$${(f \circ g)(x)} = \sqrt{\sqrt{3 - x} + 2}$$</paragraph>
      <paragraph>For $${(f \circ g)(x)} = \sqrt{\sqrt{3 - x} + 2},\sqrt{3 - x} + 2 \geq 0,$$ since the radicand of a square root must be positive. Since square roots are positive, $$\sqrt{3 - x} \geq 0,$$ or, $$3 - x \geq 0,$$ which gives a domain of $$\left( \text{-\infty} ,3 \right\rbrack$$ .</paragraph>
    </solution>
    <paragraph>This example shows that knowledge of the range of functions (specifically the inner function) can also be helpful in finding the domain of a composite function. It also shows that the domain of $$f \circ g$$ can contain values that are not in the domain of $$f,$$ though they must be in the domain of $$g.$$</paragraph>
  </example>
  <note type="practice" source_type="Try It" title="Try It" number="#6">
    <paragraph>Find the domain of</paragraph>
    <paragraph math="block">$$\left( {f \circ g} \right)(x) \text{where} f(x) = \frac{1}{x - 2} \text{and} g(x) = \sqrt{x + 4}$$</paragraph>
  </note>
  <heading level="2">Decomposing a Composite Function into its Component Functions</heading>
  <paragraph>In some cases, it is necessary to decompose a complicated function. In other words, we can write it as a composition of two simpler functions. There may be more than one way to decompose a composite function, so we may choose the decomposition that appears to be most expedient.</paragraph>
  <example number="10" title="Decomposing a Function">
    <problem>
      <paragraph>Write $$f(x) = \sqrt{5 - x^{2}}$$ as the composition of two functions.</paragraph>
    </problem>
    <solution>
      <paragraph>We are looking for two functions, $$g$$ and $$h,$$ so $$f(x) = g(h(x)).$$ To do this, we look for a function inside a function in the formula for $$f(x).$$ As one possibility, we might notice that the expression $$5 - x^{2}$$ is the inside of the square root. We could then decompose the function as</paragraph>
      <paragraph math="block">$$h(x) = 5 - x^{2} \text{and} g(x) = \sqrt{x}$$</paragraph>
      <paragraph>We can check our answer by recomposing the functions.</paragraph>
      <paragraph math="block">$$g(h(x)) = g\left( {5 - x^{2}} \right) = \sqrt{5 - x^{2}}$$</paragraph>
    </solution>
  </example>
  <note type="practice" source_type="Try It" title="Try It" number="#7">
    <paragraph>Write $$f(x) = \frac{4}{3 - \sqrt{4 + x^{2}}}$$ as the composition of two functions.</paragraph>
  </note>
  <note type="sidebar" source_type="Media" title="Media">
    <paragraph>Access these online resources for additional instruction and practice with composite functions.</paragraph>
    <list type="unordered">
      <item>Composite Functions</item>
      <item>Composite Function Notation Application</item>
      <item>Composite Functions Using Graphs</item>
      <item>Decompose Functions</item>
      <item>Composite Function Values</item>
    </list>
  </note>
</section>
