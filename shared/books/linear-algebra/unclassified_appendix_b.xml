<?xml version="1.0" encoding="UTF-8"?>
<section_file chapter="unclassified" unit_code="appendix_b">
  <appendix_b title="Appendix B: Complex Numbers">
    <heading level="1">Appendix B: Complex Numbers</heading>
    <paragraph>A <strong>complex number</strong> is a number written in the form</paragraph>
    <paragraph>
      <math display="block">$$z = a + bi$$</math>
    </paragraph>
    <paragraph>where <math display="inline">$a$</math> and <math display="inline">$b$</math> are real numbers and <math display="inline">$i$</math> is a formal symbol satisfying the relation <math display="inline">$i^{2} = - 1$</math>. The number <math display="inline">$a$</math> is the <strong>real part</strong> of <math display="inline">$z$</math>, denoted by <math display="inline">$\text{Re }z$</math>, and <math display="inline">$b$</math> is the <strong>imaginary part</strong> of <math display="inline">$z$</math>, denoted by <math display="inline">$\text{Im }z$</math>. Two complex numbers are considered equal if and only if their real and imaginary parts are equal. For example, if <math display="inline">$z = 5 + ( - 2)i$</math>, then <math display="inline">$\text{Re }z = 5$</math> and <math display="inline">$\text{Im }z = - 2$</math>. For simplicity, we write <math display="inline">$z = 5 - 2i.$</math></paragraph>
    <paragraph>A real number <math display="inline">$a$</math> is considered as a special type of complex number, by identifying <math display="inline">$a$</math> with <math display="inline">$a + 0i$</math>. Furthermore, arithmetic operations on real numbers can be extended to the set of complex numbers.</paragraph>
    <paragraph>The <strong>complex number system</strong>, denoted by <math display="inline">$\text{ℂ}$</math>, is the set of all complex numbers, together with the following operations of addition and multiplication:</paragraph>
    <paragraph>
      <math display="block">$$\boxed{\begin{array}{l}
    {(a + bi) + (c + di) = (a + c) + (b + d)i}
    \end{array}}$$</math>
    </paragraph>
    <paragraph>
      <math display="block">$$\boxed{\begin{array}{l}
    {(a + bi)(c + di) = (ac - bd) + (ad + bc)i}
    \end{array}}$$</math>
    </paragraph>
    <paragraph>These rules reduce to ordinary addition and multiplication of real numbers when <math display="inline">$b$</math> and <math display="inline">$d$</math> are zero in (1) and (2). It is readily checked that the usual laws of arithmetic for <math display="inline">$\textbf{ℝ}$</math> also hold for <math display="inline">$\textbf{ℂ}$</math>. For this reason, multiplication is usually computed by algebraic expansion, as in the following example.</paragraph>
    <example number="1">
        <heading level="2">Example 1</heading>
            <paragraph><math display="block">$$\begin{matrix}
    {(5 - 2i)(3 + 4i)} &amp; {= 15 + 20i - 6i - 8i^{2}} \\
     &amp; {= 15 + 14i - 8( - 1)} \\
     &amp; {= 23 + 14i}
    \end{matrix}$$</math></paragraph>
            <paragraph>That is, multiply each term of <math display="inline">$5 - 2i$</math> by each term of <math display="inline">$3 + 4i$</math>, use <math display="inline">$i^{2} = - 1$</math>, and write the result in the form <math display="inline">$a + bi$</math>.</paragraph>
      </example>
    <paragraph>Subtraction of complex numbers <math display="inline">$z_{1}$</math> and <math display="inline">$z_{2}$</math> is defined by</paragraph>
    <paragraph>
      <math display="block">$$z_{1} - z_{2} = z_{1} + ( - 1)z_{2}$$</math>
    </paragraph>
    <paragraph>In particular, we write <math display="inline">$- z$</math> in place of <math display="inline">$( - 1)z$</math>.</paragraph>
    <paragraph>The <strong>conjugate</strong> of <math display="inline">$z = a + bi$</math> is the complex number <math display="inline">$\overline{z}$</math> (read as “<math display="inline">$z$</math> bar”), defined by</paragraph>
    <paragraph>
      <math display="block">$$\boxed{\begin{array}{l}
    {\overline{z} = a - bi}
    \end{array}}$$</math>
    </paragraph>
    <paragraph>Obtain <math display="inline">$\overline{z}$</math> from <math display="inline">$z$</math> by reversing the sign of the imaginary part.</paragraph>
    <example number="2">
        <heading level="2">Example 2</heading>
            <paragraph>The conjugate of <math display="inline">$- 3 + 4i\,$</math> is <math display="inline">$\, - 3 - 4i$</math>; write <math display="inline">$\overline{- 3 + 4i} = - 3 - 4i$</math>.</paragraph>
      </example>
    <paragraph>Observe that if <math display="inline">$z = a + bi$</math>, then</paragraph>
    <paragraph>
      <math display="block">$$z\overline{z} = \,\,\left( {a + bi} \right)\left( {a - bi} \right)\, = a^{2} - abi + bai - b^{2}i^{2} = a^{2} + b^{2}$$</math>
    </paragraph>
    <paragraph>Since <math display="inline">$z\overline{z}$</math> is real and nonnegative, it has a square root. The <strong>absolute value</strong> (or <strong>modulus</strong>) of <math display="inline">$z$</math> is the real number <math display="inline">$|z|$</math> defined by</paragraph>
    <paragraph>
      <math display="block">$$\boxed{\begin{array}{l}
    {\text{|}z\text{|} = \sqrt{z\overline{z}} = \sqrt{a^{2} + b^{2}}}
    \end{array}}$$</math>
    </paragraph>
    <paragraph>If <math display="inline">$z$</math> is a real number, then <math display="inline">$z = a + 0i$</math>, and <math display="inline">$\text{|}z\text{|} = \sqrt{a^{2}}$</math>, which equals the ordinary absolute value of <math display="inline">$a.$</math></paragraph>
    <paragraph>Some useful properties of conjugates and absolute value are listed below; <math display="inline">$w$</math> and <math display="inline">$z$</math> denote complex numbers.</paragraph>
    <list ordered="true">
            <item>z¯=z if and only if z is a real number.</item>
            <item>w+z¯=w¯+z¯.</item>
            <item>wz¯=w¯z¯; in particular, rz¯=rz¯ if r is a real number.</item>
            <item>zz¯=z2≥0.</item>
            <item>|wz|=|w||z|.</item>
            <item>|w+z|≤|w|+|z|.</item>
      </list>
    <paragraph>If <math display="inline">$z \neq 0$</math>, then <math display="inline">$|z| &gt; 0$</math> and <math display="inline">$z$</math> has a multiplicative inverse, denoted by <math display="inline">$1/z$</math> or <math display="inline">$z^{- 1}$</math> and given by</paragraph>
    <paragraph>
      <math display="block">$$\boxed{\begin{array}{l}
    {\frac{1}{z} = z^{- 1} = \frac{\overline{z}}{|z|{}^{2}}}
    \end{array}}$$</math>
    </paragraph>
    <paragraph>Of course, a quotient <math display="inline">$w/z$</math> simply means <math display="inline">$w\,\cdot\,\,\left( {1/z} \right)$</math>.</paragraph>
    <example number="3">
        <heading level="2">Example 3</heading>
            <paragraph>Let <math display="inline">$w = 3 + 4i$</math> and <math display="inline">$z = 5 - 2i$</math>. Compute <math display="inline">$z\overline{z}$</math>, <math display="inline">$|z|$</math>, and <math display="inline">$w/z$</math>.</paragraph>
        <heading level="3">Solution</heading>
            <paragraph>From equation (3),</paragraph>
            <paragraph><math display="block">$$z\overline{z} = 5^{2} + \left( {- 2} \right){}^{2} = 25 + 4 = 29$$</math></paragraph>
            <paragraph>For the absolute value, <math display="inline">$|z| = \sqrt{z\overline{z}} = \sqrt{29}$</math>. To compute <math display="inline">$w/z$</math>, first multiply both the numerator and the denominator by <math display="inline">$\overline{z}$</math>, the conjugate of the denominator. Because of (3), this eliminates the <math display="inline">$i$</math> in the denominator:</paragraph>
            <paragraph><math display="block">$$\begin{matrix}
    \frac{w}{z} &amp; = &amp; \frac{3 + 4i}{5 - 2i} \\
     &amp; = &amp; {\frac{3 + 4i}{5 - 2i}\text{⋅}\frac{5 + 2i}{5 + 2i}} \\
     &amp; = &amp; \frac{15 + 6i + 20i - 8}{5^{2} + \left( {- 2} \right){}^{2}} \\
     &amp; = &amp; \frac{7 + 26i}{29} \\
     &amp; = &amp; {\frac{7}{29} + \frac{26}{29}i}
    \end{matrix}$$</math></paragraph>
      </example>
    <heading level="2">Geometric Interpretation</heading>
    <paragraph>Each complex number <math display="inline">$z = a + bi$</math> corresponds to a point <math display="inline">$(a,b)$</math> in the plane <math display="inline">$\textbf{ℝ}^{2}$</math>, as in <strong>Figure 1</strong>. The horizontal axis is called the <strong>real axis</strong> because the points <math display="inline">$(a,0)$</math> on it correspond to the real numbers. The vertical axis is the <strong>imaginary axis</strong> because the points <math display="inline">$(0,b)$</math> on it correspond to the <strong>pure imaginary numbers</strong> of the form <math display="inline">$0 + bi$</math>, or simply <math display="inline">$bi$</math>. The conjugate of <math display="inline">$z$</math> is the mirror image of <math display="inline">$z$</math> in the real axis. The absolute value of <math display="inline">$z$</math> is the distance from <math display="inline">$(a,b)$</math> to the origin.</paragraph>
    <figure label="Figure 1" caption="The complex conjugate is a mirror image">
        <image alt="A graph shows the complex and its complex conjugate as a mirror image."/>
      </figure>
    <paragraph>Addition of complex numbers <math display="inline">$z = a + bi$</math> and <math display="inline">$w = c + di$</math> corresponds to vector addition of <math display="inline">$(a,b)$</math> and <math display="inline">$(c,d)$</math> in <math display="inline">$\textbf{ℝ}^{2}$</math>, as in <strong>Figure 2</strong>.</paragraph>
    <figure label="Figure 2" caption="Addition of complex numbers">
        <image alt="A graph shows a tilted parallelogram plane with one of the vertices touching the origin."/>
      </figure>
    <paragraph>To give a graphical representation of complex multiplication, we use <strong>polar coordinates</strong> in <math display="inline">$\textbf{ℝ}^{2}$</math>. Given a nonzero complex number <math display="inline">$z = a + bi$</math>, let <math display="inline">$\varphi$</math> be the angle between the positive real axis and the point <math display="inline">$(a,b)$</math>, as in <strong>Figure 3</strong> where <math display="inline">$- \pi &lt; \varphi \leq \pi$</math>. The angle <math display="inline">$\varphi$</math> is called the <strong>argument</strong> of <math display="inline">$z$</math>; we write <math display="inline">$\varphi = \arg z$</math>. From trigonometry,</paragraph>
    <paragraph>
      <math display="block">$$a = |z|\cos\varphi,~~~~b = |z|\sin\varphi$$</math>
    </paragraph>
    <figure label="Figure 3" caption="Polar coordinates of  z">
        <image alt="A graph shows a right angled triangle with all the edges giving the polar coordinates of a complex number denoted by the letter z."/>
      </figure>
    <paragraph>and so</paragraph>
    <paragraph>
      <math display="block">$$\left. z = a + bi = \middle| z \middle| (\cos\,\varphi + i\sin\varphi) \right.$$</math>
    </paragraph>
    <paragraph>If <math display="inline">$w$</math> is another nonzero complex number, say,</paragraph>
    <paragraph>
      <math display="block">$$\left. w = \middle| w \middle| ~(\cos\,\mspace{-4mu}\,\mspace{-4mu}\vartheta + i\sin\vartheta) \right.$$</math>
    </paragraph>
    <paragraph>then, using standard trigonometric identities for the sine and cosine of the sum of two angles, one can verify that</paragraph>
    <paragraph>
      <math display="block">$$\left. wz = \middle| w \middle| ~ \middle| z \middle| ~\left\lbrack \text{cos}(\vartheta + \varphi)~ + ~i\sin(\vartheta + \varphi) \right\rbrack \right.$$</math>
    </paragraph>
    <paragraph>See <strong>Figure</strong><strong>4</strong>. A similar formula may be written for quotients in polar form. The formulas for products and quotients can be stated in words as follows.</paragraph>
    <figure label="Figure 4" caption="Multiplication with polar coordinates">
        <image alt="A vector graph shows two vectors lying in the first quadrant and their product in the second quadrant along with their respective angles from the positive horizontal axis."/>
      </figure>
    <note type="theorem_key">
            <paragraph>The product of two nonzero complex numbers is given in polar form by the product of their absolute values and the sum of their arguments. The quotient of two nonzero complex numbers is given by the quotient of their absolute values and the difference of their arguments.</paragraph>
      </note>
    <example number="4">
        <heading level="2">Example 4</heading>
        <list ordered="true">
                <item>If w has absolute value 1, then w=cos ϑ+isin ϑ, where ϑ is the argument of w. Multiplication of any nonzero number z by w simply rotates z through the angle ϑ.</item>
                <item>The argument of i itself is π/2 radians, so multiplication of z by i rotates z through an angle of π/2 radians. For example, 3+i is rotated into (3+i)i=−1+3i.</item>
        </list>
        <figure>
          <image alt="A vector graph shows a point i on the vertical axis and two vectors lying in the first and second quadrants respectively along with their angles in from the positive horizontal axis."/>
        </figure>
      </example>
    <heading level="2">Powers of a Complex Number</heading>
    <paragraph>Formula (4) applies when <math display="inline">$z = w = r\,\mspace{-4mu}\text{(}\cos\,\varphi + i\sin\varphi\text{)}$</math>. In this case</paragraph>
    <paragraph>
      <math display="block">$$z^{2} = r^{2}\text{(cos~}2\,\varphi + i\sin 2\,\varphi\text{)}$$</math>
    </paragraph>
    <paragraph>and</paragraph>
    <paragraph>
      <math display="block">$$\begin{matrix}
    z^{3} &amp; = &amp; {z \cdot z^{2}} \\
     &amp; = &amp; {r\text{(cos}\,\varphi + i\sin\,\varphi\text{)} \cdot r^{2}\text{(cos}\,2\varphi + i\sin\,2\varphi\text{)}} \\
     &amp; = &amp; {r^{3}\text{(cos}\,3\varphi + i\sin\,3\varphi\text{)}}
    \end{matrix}$$</math>
    </paragraph>
    <paragraph>In general, for any positive integer <math display="inline">$k$</math>,</paragraph>
    <paragraph>
      <math display="block">$$z^{k} = r^{k}\left( {\cos k\varphi + i\sin k\varphi} \right)$$</math>
    </paragraph>
    <paragraph>This fact is known as <em>De Moivre’s Theorem</em>.</paragraph>
    <heading level="2">Complex Numbers and ℝ2</heading>
    <paragraph>Although the elements of <math display="inline">$\textbf{ℝ}^{2}$</math> and <math display="inline">$\mathbf{\mathbb{C}}$</math> are in one-to-one correspondence, and the operations of addition are essentially the same, there is a logical distinction between <math display="inline">$\textbf{ℝ}^{2}$</math> and <math display="inline">$\textbf{ℂ}$</math>. In <math display="inline">$\textbf{ℝ}^{2}$</math> we can only multiply a vector by a real scalar, whereas in <math display="inline">$\textbf{ℂ}$</math> we can multiply any two complex numbers to obtain a third complex number. (The dot product in <math display="inline">$\textbf{ℝ}^{2}$</math> doesn’t count, because it produces a scalar, not an element of <math display="inline">$\textbf{ℝ}^{2}$</math>.) We use scalar notation for elements in <math display="inline">$\textbf{ℂ}$</math> to emphasize this distinction.</paragraph>
    <figure>
        <image alt="The figure illustrates a real coordinate plane R squared and a complex coordinate plane C, displaying the plot of ordered pairs and complex numbers, respectively."/>
      </figure>
  </appendix_b>
</section_file>