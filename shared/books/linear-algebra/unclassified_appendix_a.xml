<?xml version="1.0" encoding="UTF-8"?>
<section_file chapter="unclassified" unit_code="appendix_a">
  <appendix_a title="Appendix A: Uniqueness of the Reduced Echelon Form">
    <heading level="1">Appendix A: Uniqueness of the Reduced Echelon Form</heading>
    <note type="procedure" title="Theorem ﻿﻿">
        <heading level="2">Theorem ﻿﻿</heading>
        <heading level="3">﻿Uniqueness of the Reduced Echelon Form﻿</heading>
            <paragraph>Each <math display="inline">$m \times n$</math> matrix <math display="inline">$A$</math> is row equivalent to a unique reduced echelon matrix <math display="inline">$U$</math>.</paragraph>
      </note>
    <heading level="3">Proof﻿</heading>
    <paragraph>The proof uses the idea from Section 4.3 that the columns of row-equivalent matrices have exactly the same linear dependence relations.</paragraph>
    <paragraph>The row reduction algorithm shows that there exists at least one such matrix <math display="inline">$U$</math>. Suppose that <math display="inline">$A$</math> is row equivalent to matrices <math display="inline">$U$</math> and <math display="inline">$V$</math> in reduced echelon form. The leftmost nonzero entry in a row of <math display="inline">$U$</math> is a “leading <math display="inline">$l$</math>.” Call the location of such a leading <math display="inline">$1$</math> a pivot position, and call the column that contains it a pivot column. (This definition uses only the echelon nature of <math display="inline">$U$</math> and <math display="inline">$V$</math> and does not assume the uniqueness of the reduced echelon form.)</paragraph>
    <paragraph>The pivot columns of <math display="inline">$U$</math> and <math display="inline">$V$</math> are precisely the nonzero columns that are <em>not</em> linearly dependent on the columns to their left. (This condition is satisfied automatically by a <em>first</em> column if it is nonzero.) Since <math display="inline">$U$</math> and <math display="inline">$V$</math> are row equivalent (both being row equivalent to <math display="inline">$A$</math>), their columns have the same linear dependence relations. Hence, the pivot columns of <math display="inline">$U$</math> and <math display="inline">$V$</math> appear in the same locations. If there are <math display="inline">$r$</math> such columns, then since <math display="inline">$U$</math> and <math display="inline">$V$</math> are in reduced echelon form, their pivot columns are the first <math display="inline">$r$</math> columns of the <math display="inline">$m \times m$</math> identity matrix. Thus, <em>corresponding pivot columns of</em><math display="inline">$U$</math><em>and</em><math display="inline">$V$</math><em>are equal</em>.</paragraph>
    <paragraph>Finally, consider any nonpivot column of <math display="inline">$U$</math>, say column <math display="inline">$j$</math>. This column is either zero or a linear combination of the pivot columns to its left (because those pivot columns are a basis for the space spanned by the columns to the left of column <math display="inline">$j$</math>). Either case can be expressed by writing <math display="inline">$U\textbf{x} = \textbf{0}$</math> for some <math display="inline">$\textbf{x}$</math> whose <math display="inline">$j\text{th}$</math> entry is <math display="inline">$1$</math>. Then <math display="inline">$V\textbf{x} = \textbf{0}$</math>, too, which says that column <math display="inline">$j$</math> of <math display="inline">$V$</math> is either zero or the <em>same</em> linear combination of the pivot columns of <math display="inline">$V$</math> to <em>its</em> left. Since corresponding pivot columns of <math display="inline">$U$</math> and <math display="inline">$V$</math> are equal, columns <math display="inline">$j$</math> of <math display="inline">$U$</math> and <math display="inline">$V$</math> are also equal. This holds for all nonpivot columns, so <math display="inline">$V = U$</math>, which proves that <math display="inline">$U$</math> is unique.</paragraph>
  </appendix_a>
</section_file>