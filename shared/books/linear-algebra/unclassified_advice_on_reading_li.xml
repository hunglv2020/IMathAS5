<?xml version="1.0" encoding="UTF-8"?>
<section_file chapter="unclassified" unit_code="advice_on_reading_li">
  <advice_on_reading_li title="Advice on Reading Linear Algebra">
    <heading level="1">Advice on Reading Linear Algebra</heading>
    <paragraph>When you are reading linear algebra, you will encounter several different types of mathematical objects. Here are some conventions that will help you to keep track of which objects are which.</paragraph>
    <list ordered="false">
            <item>Pay attention to case and font. Mathematical objects are usually bold or italic, and “A,” “A,” “a,” and “a” may be used to represent four different mathematical objects at the same time, while “A” and “a” are one-letter words.</item>
            <item>Pay attention to subscripts and super scripts, which also give you clues about the objects you are reading about.</item>
            <item>Repeated letters in the same font and case are significant. An n×n matrix has the same number of rows as columns, whereas an m×n matrix may or may not have the same number of rows as columns.</item>
            <item>As you read, it may help to imagine shapes and pictures. I think of a matrix as a large, wide rectangle full of numbers and a vector as a tall, thin rectangle of numbers.</item>
      </list>
    <paragraph>Finally, read the surrounding text carefully as the type of object you are reading (or writing) about should be clearly stated, and the conventions in this list are <em>not</em> always followed. When you are writing by hand, it is particularly important to indicate what each letter represents.</paragraph>
    <paragraph><em><strong>Warning:</strong></em><strong/>This list is just a set of conventions. Always read the surrounding text to see which objects are being represented by each letter, case, and font.</paragraph>
    <paragraph>Italicized lowercase letters usually represent numbers. Pay attention to the context to determine whether they are integers, real numbers, or complex numbers.</paragraph>
    <paragraph>Italicized capital letters near the beginning of the alphabet are frequently used to represent matrices.</paragraph>
    <paragraph>When a matrix is listed as an italicized capital letter, the entries in the matrix are frequently represented using the same letter, but in lowercase italic with two subscripts. The first subscript tells you the row number. The second subscript tells you the column number. So <math display="inline">$a_{23}$</math> ﻿is the entry in the second row and third column of matrix <math display="inline">$A$</math>, whereas <math display="inline">$b_{31}$</math> is the entry in the third row and first column of the matrix <math display="inline">$B$</math>.﻿</paragraph>
    <paragraph>When you see an italicized capital letter with two subscripts, you need to check the surrounding text to see how this notation is being used. For example, in the context of Section 2.4, a matrix <math display="inline">$A$</math> has been partitioned into submatrices and <math display="inline">$A_{34}$</math> donates the submatrix in the third row and fourth column of this block partitioning. In contrast, in Section 3.1, <math display="inline">$A_{34}$</math> represents the matrix that was created by deleting the third row and fourth column of the matrix <math display="inline">$A$</math>.</paragraph>
    <paragraph>When we have a list of matrices, we may use the same italicized capital letter with a single subscript. A different subscript indicates a different matrix. The letter <math display="inline">$E$</math> is used for special matrices such as the elementary matrices introduced in Chapter 2. Other special matrices may also be represented by the first letter of their description—so a diagonal matrix might be called <math display="inline">$D$</math>.</paragraph>
    <paragraph>An italicized capital <math display="inline">$T$</math> usually represents a linear transformation. If more than one transformation is needed, then <math display="inline">$S$</math> or <math display="inline">$U$</math> is a frequent second choice.</paragraph>
    <paragraph>Vectors are frequently listed in this text with bold, but not italic, letters. They are often chosen to be near the end of the alphabet; however, <math display="inline">$\mathbf{b}$</math> is used for vectors that represent the right-hand side of equations. In higher-level linear algebra or math textbooks, they may be italicized, but not bold. In calculus and physics books, vectors may be written with an arrow over top of them.</paragraph>
    <paragraph>The entries in a vector may be represented using the same letter as the vector, but these letters will be italicized (not bold), with one subscript. The subscript describes the position of the entry. For example, <math display="inline">$v_{4}$</math> is the fourth entry in the vector <math display="inline">$\mathbf{v}$</math>.</paragraph>
    <paragraph>This one gets a little tricky too. To list several vectors, bold letters with a single subscript are often used. Thus, <math display="inline">$\mathbf{v}_{1}$</math>, <math display="inline">$\mathbf{v}_{2}$</math>, and <math display="inline">$\mathbf{v}_{3}$</math> are three different vectors, whereas <math display="inline">$v_{1}$</math>, <math display="inline">$v_{2}$</math>, and <math display="inline">$v_{3}$</math> are the first three entries in a vector named <math display="inline">$\mathbf{v}$</math>. Clearly, we need to move to a different convention if we want to describe the entries of <math display="inline">$\mathbf{v}_{2}$</math>.</paragraph>
    <paragraph>A bold <math display="inline">$\mathbf{e}$</math> is usually associated with the standard basis vectors, with the subscript indicating which row contains a <math display="inline">$1$</math>. Thus, <math display="inline">$\mathbf{e}_{2}$</math> is a vector with a <math display="inline">$1$</math> in the second row and zeros in all the other rows. You will have to read the surrounding text to tell how many zero entries are in <math display="inline">$\mathbf{e}_{2}$</math>.</paragraph>
    <paragraph>An italicized <math display="inline">$I$</math> always represents an identity matrix. Sometimes we indicate its size using a subscript. For example, <math display="inline">$I_{3}$</math> is the identity matrix with three rows and columns, while <math display="inline">$I_{n}$</math> represents an <math display="inline">$n \times n$</math> identity matrix. But if the letter has not been italicized, I just represents me!</paragraph>
    <paragraph>An <math display="inline">$n \times n$</math> matrix with superscript <math display="inline">$- 1$</math> denotes the inverse of that matrix. When matrix <math display="inline">$A$</math> has an inverse, it has the special property that <math display="inline">$A^{- 1}A = I = AA^{- 1}$</math>. What may be different from what you have seen before is that we do <em>not</em> write﻿ <math display="inline">$1/A$</math>, ﻿when <math display="inline">$A$</math> is a matrix.</paragraph>
    <paragraph>An <math display="inline">$n \times n$</math> matrix with a superscript positive integer works much like the exponents you have encountered in other math classes. For example, <math display="inline">$A^{1} = A$</math>, <math display="inline">$A^{2} = A(A)$</math>, <math display="inline">$A^{3} = A(A)(A)$</math>, and so on. We also define <math display="inline">$A^{0} = I$</math>, the identity matrix. For negative superscripts, use the inverse. For example <math display="inline">$A^{- 2} = \left( A^{- 1} \right)^{2} = A^{- 1}\left( A^{- 1} \right)$</math>, <math display="inline">$A^{- 3} = \left( A^{- 1} \right)^{3} = A^{- 1}\left( A^{- 1} \right)\left( A^{- 1} \right)$</math>, and so on.﻿</paragraph>
    <paragraph>Be careful. When the superscript is an italicized capital <math display="inline">$T$</math>, it represents the transpose of the matrix named with the same letter. Thus, <math display="inline">$A^{T}$</math> is the matrix formed from the matrix <math display="inline">$A$</math> by switching the rows with the columns.</paragraph>
    <paragraph>Letters where parts of the font are double barred represent specific number or mathematical systems. In particular, <math display="inline">$\mathbb{R}$</math> always represents the real numbers.﻿</paragraph>
    <paragraph>When we want to represent the set of all vectors of the same size with real entries, we use <math display="inline">$\mathbb{R}$</math> with an integer superscript greater than <math display="inline">$1$</math>, where the integer gives the common size of all the vectors. For example, <math display="inline">${\mathbb{R}}^{2}$</math> indicates that we are talking about the set of all vectors with two entries, both of which are real numbers, and <math display="inline">${\mathbb{R}}^{3}$</math> indicates that we are talking about the set of all vectors with three real number entries. When you encounter an <math display="inline">${\mathbb{R}}^{n}$</math>, you know that all the vectors in the set have <math display="inline">$n$</math> real entries. In this case, you should look for other objects in the same area described using the letter <math display="inline">$n$</math> as well.</paragraph>
    <paragraph>An italicized <math display="inline">$V$</math> is usually chosen to represent a vector space, with an italicized <math display="inline">$H$</math> representing a subspace.</paragraph>
    <paragraph>Capital letters in a script font are often used to name a basis.</paragraph>
    <paragraph>A vector inside square brackets with a subscript script letter usually represents the vector of coefficients formed when writing that vector in terms of the basis named by the script subscript.</paragraph>
    <paragraph>Greek letters frequently represent numbers that are playing the special role of being eigenvalues.</paragraph>
    <paragraph>When two matrices (or a matrix and a vector) are written side-by-side, the implied operation is matrix multiplication. Remember <math display="inline">$AB \neq BA$</math>, most of the time. When a grouping symbol is needed, the preferred choice is parentheses: <math display="inline">$A(B + C)$</math> <em>or</em> <math display="inline">$(A)(B + C)$</math>. ﻿</paragraph>
    <paragraph>In this text, the symbol <math display="inline">$\times$</math> is used to identify the size of a matrix. For example, a <math display="inline">$2 \times 3$</math> matrix has two rows and three columns. In Chapter 8, <math display="inline">$\times$</math> is used to represent the cross product between two vectors.﻿</paragraph>
    <paragraph>When you see a raised dot, pay attention to the context. A raised dot between two real numbers represents the usual arithmetic product: <math display="inline">$3 \cdot 5 = 15$</math>. ﻿When it is used between two vectors, such as in Chapter 6, it indicates the inner (or dot) product of the two vectors.</paragraph>
    <paragraph>The symbol <math display="inline">$\bot$</math>, referred to as the perp symbol, represents orthogonality. When you see it used as a superscript, it refers to all the vectors orthogonal to the given vector or set of vectors.</paragraph>
    <paragraph>Single bars usually surround a number and indicate absolute value. Double bars usually surround a vector and represent the length or norm of the vector.</paragraph>
  </advice_on_reading_li>
</section_file>