# Canonical active-question manifest.
# Missing file or missing `active_qt` means no active question is selected.
# Accepted format: `qt-<digits>`.
# Invalid values are contract errors.
schema_version = 1
active_qt = "qt-219068"
